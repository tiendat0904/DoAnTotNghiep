import {ExportDirective} from './export.directive';

describe('ExportDirective', () => {
  it('should create an instance', () => {
    let directive: ExportDirective;
    // @ts-ignore
    directive = new ExportDirective();
    expect(directive).toBeTruthy();
  });
});
